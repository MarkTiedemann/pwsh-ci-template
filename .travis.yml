matrix:
  include:
    - language: sh
      os:
        - linux
      before_script:
        - chmod +x ./ci/install_linux.sh
        - ./ci/install_linux.sh
      script:
        - pwsh -c \$PSVersionTable
    - language: sh
      os:
        - osx
      before_script:
        - chmod +x ./ci/install_osx.sh
        - ./ci/install_osx.sh
      script:
        - pwsh -c \$PSVersionTable
